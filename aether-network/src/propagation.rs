//! Block propagation

/// Block propagator
pub struct BlockPropagator;

impl BlockPropagator {
    /// Propagate block to peers
    pub fn propagate() {
        // TODO: Implement propagation
    }
}
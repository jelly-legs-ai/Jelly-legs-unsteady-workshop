[package]
name = "aether-tokens"
version = "0.1.0"
edition = "2021"
description = "FLUX and AETH token contracts for AeTHer Chain"

[dependencies]
solana-program = "1.18"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
client = []

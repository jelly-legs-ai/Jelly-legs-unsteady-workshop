# Memory — 2026-04-06

## Critical System Change: GitHub Actions DISABLED for AI Work

### What We Fixed Tonight

GitHub Actions cannot do AI work — it was only posting placeholder spam comments to issues. All real AI work is now confirmed running through ME (Jelly-legs) via local OpenClaw cron.

**Evidence:**
- All real comments on issues #114, #115, #116 are posted by `jelly-legs-ai` (my subagents)
- `github-actions[bot]` was posting placeholder "Cycle update" comments from the disabled `ai-team-real-work.yml`

**Actions taken:**
1. Renamed GitHub workflows to `.DISABLED`:
   - `.github/workflows/agent-orchestrator-v3.yml.DISABLED`
   - `.github/workflows/ai-team-real-work.yml.DISABLED`
2. Committed as `618d888` — "fix: disable placeholder GitHub Actions workflows"

### My Local Cron: The Only Working AI System

**Job ID:** `94440797-4584-4f24-8526-e498cdcfb6e1`  
**Name:** "AI Team - 10min Persistent Issues Cycle" (needs renaming to "7min")  
**Schedule:** Every 7 minutes (420,000ms) — updated from 10min  
**Delivery:** `mode: "none"` — no announcements, just silent work

**Payload — 3 subagents spawned in parallel:**
- **#115 (Blockchain):** Model `qwen3.5:397b-cloud`, workspace `Jelly-legs-unsteady-workshop`
- **#114 (Website):** Model `kimi-k2.5:cloud`, workspace `aether-site`
- **#116 (CLI):** Model `gemma3:27b-cloud`, workspace `aether-cli`

Each agent: pulls git, makes ONE code change, commits+pushes, returns summary → I post to GitHub as `jelly-legs-ai`

### Pending uncommitted (low priority):
- Submodule updates: `Jelly-legs-unsteady-workshop`, `aether`, `aether-site`, `aether-cli/index.js`, `aether-cli/commands/tx-history.js`
- These are submodule pointer updates — not critical right now

### Issue #116 SDK Buildout (Added 2026-04-06)
- Issue #116 updated with SDK buildout directive
- All CLI commands should map to installable SDK packages
- Package name: `@jellylegsai/aether-sdk` (core + staking + validators + governance + tokens)
- Quick install: `aether sdk install` (runs npm install in user dir)
- CLI command `aether sdk` should do the install, not just show docs
- **CRITICAL: Each SDK function must be CODED TO THE BLOCKCHAIN — real RPC calls, real TX submission, no stubs**
- Updated issue #116 body with explicit "wired to real chain" requirement
- Comment posted to #116 explaining the "coded to the blockchain" requirement

### Gateway Status
- Gateway: running on port 18789, loopback only
- Last crash/restart: around 23:24 UTC (from openclaw gateway status)
- Health check: `curl.exe -s http://127.0.0.1:18789/health` → `{"ok":true,"status":"live"}`

### HEARTBEAT.md Updated
- Reflects new architecture: GitHub Actions disabled, all AI work via local cron
- Lists the 4 working subagent models (minimax-m2.7:cloud, qwen3.5:397b-cloud, kimi-k2.5:cloud, gemma3:27b-cloud)
- Cron fires every 7 min, spawns 3 parallel agents

### HEARTBEAT.md Still Needs Push
- HEARTBEAT.md modified but not yet pushed to origin
- The `.DISABLED` files also modified but push pending (git pull --rebase needed first)

# Memory — 2026-04-05

## Persistent Issues System — REBUILT TODAY

### What happened
- GitHub Actions `ai-team-real-work.yml` was spamming placeholder comments every 5 min, drowning out real jelly-legs-ai comments
- Disabled all spam workflows: `ai-team-real-work.yml`, `issue-114-*.yml`, `issue-115-*.yml`, `issue-116-*.yml`
- Set up HEARTBEAT.md as the engine — I wake every ~30 min and spawn agents

### Model limitations discovered
Only 2 subagent models work from this OpenClaw setup:
- `minimax-m2.7:cloud` 
- `qwen3.5:397b-cloud`
- GLM-5, Gemma4, Deepseek not in allowed subagent list (need openclaw.json config update)

### How it works now
- I am the mastermind (spawn agents, post to GitHub)
- Cron wakes me every ~30 min via heartbeat
- I spawn agents: minimax → #115 blockchain, qwen → #114 web + #116 CLI
- They commit real code, I post detailed cycle comments to each issue

### Cycle results today
- **#115 blockchain**: StateDB panic fix (bs58 addr decode), RPC endpoints verified clean build
- **#116 CLI**: wallet stake-positions, wallet export, aether-hub@1.1.5 published
- **#114 website**: Build broken — page.tsx SWC syntax error (pre-existing in committed code)

## Build Error — page.tsx SWC FAILURE

### Symptoms
- Next.js build fails with "Syntax Error" on `page.tsx:2057`
- TypeScript also reports cascading JSX errors (StaggeredGrid, div, section unclosed)
- File IS correct syntactically — 4470 lines, proper JSX, valid UTF-8
- Errors appear in multiple files: page.tsx, whitepaper/page.tsx, liquidity/page.tsx
- Previous commits (adb0e10, f2d3f1b) also had this error
- Started after "Sprint 90" commits adding heavy components (GlobalNetworkMap, etc.)

### What tried
- `git checkout HEAD -- src/app/page.tsx` — file restored but error persists
- `swcMinify: true` in next.config — didn't fix
- Removed .next cache — didn't fix
- No raw byte anomalies, no orphan CR, valid UTF-8
- Same error exists in committed git history (not caused by local changes)

### Files affected
- `src/app/page.tsx` — line 2057: `return (` inside useEffect, SWC confused
- `src/app/whitepaper/page.tsx` — cascading JSX errors  
- `src/app/liquidity/page.tsx` — also showing syntax error at line 1 (cascading from page.tsx?)

## CLI Fixes Pushed
- `validator-start.js` — fixed parseArgs to accept `{testnet, tier}` options object
- Banner now shows "TESTNET" (yellow) when testnet=true, "MAINNET" (red) otherwise  
- Build function uses `execSync` WITHOUT `shell:true` — avoids Windows spawnSync cmd.exe ENOENT
- `init.js` passes `{testnet:true, tier}` to validatorStart()

## Validator Status
- Running on `http://127.0.0.1:8899` — slot ~37,000+
- Chain ID: aether-testnet-1
- Observer tier (not Full — genesis only has 0.1 AETH)

## Untracked junk files in aether-site root
Many debug scripts were created during investigation: `check_brackets.js`, `fix_ls.js`, `jsx_check.js`, `page_minimal.tsx`, etc. — should be cleaned up.

---

## Evening Session — 2026-04-05 (~17:00–19:18 UTC)

### aether-site Build Errors — FIXED

Multiple cascading build errors in aether-site, all resolved:

1. **globals.css dangling brace** — Line 506 had `  }` and `}` orphaned after `/* Sprint 32 End */`. Fixed by removing the extra closing braces. Was in committed code (git log shows).

2. **agents/stats/route.ts** — Imported `loadAgents` from non-existent `../route`. Removed the import and replaced with empty array.

3. **next.config.js** — Missing `outputFileTracing: false` caused ENOENT on `pages/_app.js.nft.json`. Added `outputFileTracing: false`.

4. **CLI doctor binary path** — `checkValidatorBinary()` looked for `target/debug/aether-validator.exe` but release binary lives at `target/release/aether-validator.exe`. Fixed path resolution:
   - Now uses `USERPROFILE/.openclaw/workspace/Jelly-legs-unsteady-workshop` as primary path
   - Walks up from npm package location to find real workspace
   - Prefers release binary over debug

5. **CLI fix commands** — Also updated `getFixCommand()` for Validator Binary to use `--release` flag.

### Agent System Status — FULLY OPERATIONAL

Two workflows active and working:

- **`🤖 AI Team Orchestrator v3`** — every 7 min (cron `*/7 * * * *`)
  - Spawns OpenClaw agents via `POST /api/sessions/spawn`
  - All agent model references updated to only `minimax-m2.7:cloud` and `qwen3.5:397b-cloud`

- **`AI Team - Real Work`** — every 5 min (cron `*/5 * * * *`)
  - Posts detailed cycle updates to GitHub issues (no more "Scope:" spam)
  - Updates now include: chain status, recent commits, current scope
  - All agent models updated to allowed models only

### GitHub Issues — Active Persistent Issues

- **#114** (Website) — Labels: `build`, `in-progress` — Orchestrated by Sketch-Bot/Developer
- **#115** (Blockchain) — Labels: `build`, `in-progress` — Orchestrated by Code-Crafter
- **#116** (CLI) — Labels: `build`, `in-progress` — Orchestrated by Launch-Pad/Developer

### NPM Packages Updated

- **aether-hub v1.1.8** — Includes doctor.js binary path fix
  - Install: `npm install -g aether-hub`
  - Also published as `@jellylegsai/aether-validator-cli@1.0.5`

### Commits Pushed Today (afternoon/evening)
- `18418e5` fix: resolve multiple build errors - globals.css dangling brace, stats route loadAgents, next.config tracing
- `570d4c7` fix: post detailed cycle updates with commits and chain status
- `4e62f40` 🤖 Auto-update dashboard data
- `4670b9b` fix: resolve validator binary path from USERPROFILE/.openclaw/workspace
- `84bd15b` 🤖 Auto-update dashboard data
- `393b2d4` feat(rewards): add 'rewards pending' subcommand
- `60c8cba` fix(tokens): remove unused imports
- `0d4b916` feat: add wallet export command with --mnemonic and --json support
- `3590ab2` feat: add aether account command for on-chain account data queries

### Build Status
- **aether-site**: ✅ Builds successfully (52/52 pages generated)
  - Note: Flux Staking Dashboard shows "Dynamic server usage" warning — API routes use `request.url` which prevents full static generation. This is a warning, not an error. Routes correctly marked as `λ` (Dynamic) in build output.
- **CLI doctor**: ✅ Binary path resolution fixed — `aether doctor` will now find the correct binary location

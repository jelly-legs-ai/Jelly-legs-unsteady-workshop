name: AI Team - Real Work

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:

# DISABLED — This workflow posts placeholder comments. 
# All real work is done by spawned OpenClaw agents via agent-orchestrator-v3.yml
# Set enabled: false after testing
enabled: false

jobs:
  real-work:
    runs-on: ubuntu-latest
    if: false  # Disable all runs
    permissions:
      contents: write
      issues: write
      pull-requests: write
    
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Real work is done via agent-orchestrator-v3.yml"

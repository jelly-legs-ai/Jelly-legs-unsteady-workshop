## 🌐 Cycle Update — 2026-04-06 14:52 UTC

**What was done:** Fixed SDK path in `src/app/api/claim/route.ts` — was pointing to `../../aether-cli/sdk/index.js` (2 levels), corrected to `../../../aether-cli/sdk/index.js` (3 levels from `api/claim/`). Build passes cleanly.

**Commit:** `d0090cb` — `fix(#114): correct SDK path in claim API route`

**Build:** ✅ npm run build passes

**Next:** Refactor API routes to use direct `fetch()` RPC calls instead of requiring local SDK file — eliminates "SDK not available" warnings, removes complex relative path resolving, makes the site work out-of-the-box without local SDK files.
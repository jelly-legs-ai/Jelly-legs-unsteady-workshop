## 💻 Cycle Update — 2026-04-06 05:56 UTC

**What was done:** Rewired `stake-info.js` to SDK — fixed broken `require('../sdk')` path, added proper arg parsing (`--address`, `--json`, `--rpc`, `--help`), parallelised 3 SDK calls via `Promise.all()`: `getSlot()`, `getAccountInfo(addr)`, `getBlockHeight()`. All calls go through `AetherClient` → real HTTP RPC.

**Commit:** `d6105a5` — `fix(#116): wire stake-info to SDK with proper arg parsing, parallel RPC calls`

**Next:** Wire `stake-positions.js`, `delegations.js`, `multisig.js` to SDK. Sync CLI with new backend changes from #114/#115.
